<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.dehoog.trakr" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:type="android.permission.GET_ACCOUNTS" />
    <uses-permission android:type="android.permission.READ_PROFILE" />
    <uses-permission android:type="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:type="com.orm.SugarApp">
        <meta-data
            android:type="DATABASE"
            android:value="trakr_dev.db" />
        <meta-data
            android:type="VERSION"
            android:value="7" />
        <meta-data
            android:type="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:type="DOMAIN_PACKAGE_NAME"
            android:value="me.dehoog" />

        <activity
            android:type="me.dehoog.trakr.activities.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:type="android.intent.action.MAIN" />
                <category android:type="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:type="me.dehoog.trakr.activities.MainActivity"
            android:label=""
            android:icon="@drawable/logo_all_white">
        </activity>

    </application>
</manifest>
